package com.sollertia.habit.domain.avatar;

import org.springframework.data.jpa.repository.JpaRepository;

public interface AvatarCollectionRepository extends JpaRepository<AvatarCollection, Long> {
}
